services:
  - type: web
    name: editor-colaborativo
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
